import React from "react";

import classes from "../../styles/components/spinner.module.scss";

const Spinner = (props) => {
  return <div className={classes.loader}>Loading...</div>;
};

export default Spinner;
